#!/bin/bash -e

# Run migrations before starting the Rails server (not Sidekiq or one-off tasks).
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
